window.addEventListener('load', (event) => {
    return document.getElementById('front-page-logo').style.animation = "null";
});

/*
const ready = function() {
    document.getElementById('front-page-logo').style.animation = "null";
};

document.addEventListener('DOMcontentloaded', ready)
*/